openapi: 3.1.0
info:
  title: External Anchor Ref API
  version: 1.0.0
paths:
  /pets:
    get:
      operationId: get_pet_summary
      responses:
        '200':
          description: ok
          content:
            application/json:
              schema:
                $ref: './schemas/anchors.yaml#/PetSummary'
