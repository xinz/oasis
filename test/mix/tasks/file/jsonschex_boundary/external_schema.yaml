openapi: 3.1.0
info:
  title: External Schema Boundary
  version: 1.0.0
paths:
  /users:
    post:
      operationId: createUser
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: './schemas/user.yaml'
